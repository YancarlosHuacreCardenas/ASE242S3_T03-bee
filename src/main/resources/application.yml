server:
  port: ${PORT:8080}
  address: 0.0.0.0
spring:
  datasource:
    url: jdbc:sqlserver://3.233.215.100:1433;databaseName=AltavistaDB;encrypt=false;trustServerCertificate=true
    username: sa
    password: MiPassword123!
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
